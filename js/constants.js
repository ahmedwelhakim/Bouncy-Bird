
const FPS = 60;

const GRAVITY = 2500.0 / (FPS*FPS); // (px/frame^2)

const SPEED_X = 200.0 / FPS; // (px/frame)


const MIN_WIDTH = 400;
const MAX_WIDTH = 1000;
const MIN_HEIGHT = 600;
const MAX_HEIGHT = 1080;

